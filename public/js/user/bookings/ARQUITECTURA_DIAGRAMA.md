# Diagrama de Arquitectura - Sistema de Reservas Refactorizado

## 🏛️ Vista General de la Arquitectura

```
┌─────────────────────────────────────────────────────────────────────┐
│                         BOOKINGS APP                                │
│                     (bookingsApp.js)                                │
│                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │              Inicialización                                   │  │
│  │  useEffect(() => loadServicesAction())                       │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │              Renderizado Condicional                          │  │
│  │  - ServiceForm (estado === "ServiceForm")                    │  │
│  │  - DateForm (estado === "DateForm")                          │  │
│  │  - ConfirmationForm (estado === "ConfirmationForm")          │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │              BookingNavigation                                │  │
│  │  (Navegación global con flechas)                             │  │
│  └──────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              │ consume
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    BOOKINGS STORE (bookingsStore.js)                │
│                      ★ ÚNICA FUENTE DE VERDAD ★                     │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  📦 MAPS AGRUPADOS                                                  │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │ $bookingDraft = map({                                         │ │
│  │   serviceId: null,                                            │ │
│  │   service: null,                                              │ │
│  │   dia: new Date(),                                            │ │
│  │   especialista: null,                                         │ │
│  │   hora: null                                                  │ │
│  │ })                                                            │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │ $pagination = map({                                           │ │
│  │   current: 1,                                                 │ │
│  │   pageSize: 2,                                                │ │
│  │   totalPages: 0                                               │ │
│  │ })                                                            │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │ $uiState = map({                                              │ │
│  │   loading: false,                                             │ │
│  │   error: null                                                 │ │
│  │ })                                                            │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                                                     │
│  ⚡ COMPUTED STORES                                                 │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │ $mes = computed($bookingDraft, (draft) => draft.dia)         │ │
│  │ ↳ Se actualiza automáticamente cuando $bookingDraft.dia      │ │
│  │   cambia. NO necesita sincronización manual.                 │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                                                     │
│  📋 ÁTOMOS SIMPLES                                                  │
│  • $estado: atom("ServiceForm")                                    │
│  • $services: atom([])                                             │
│  • $especialistas: atom([])                                        │
│  • $userName: atom("Usuario")                                      │
│  • $totalEspecialistas: atom(0)                                    │
│                                                                     │
│  🎬 ACCIONES CENTRALIZADAS                                          │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │ Inicialización:                                               │ │
│  │  • loadServicesAction()                                       │ │
│  │  • loadUserAction()                                           │ │
│  ├───────────────────────────────────────────────────────────────┤ │
│  │ Selección de Servicio:                                        │ │
│  │  • selectServiceAction(service)                               │ │
│  │    ↳ Actualiza $bookingDraft.service                         │ │
│  │    ↳ Navega a DateForm automáticamente                       │ │
│  ├───────────────────────────────────────────────────────────────┤ │
│  │ Gestión de Fecha:                                             │ │
│  │  • setMesAction(fecha)                                        │ │
│  │  • setDiaAction(dia)                                          │ │
│  │    ↳ Actualiza $bookingDraft.dia                             │ │
│  │    ↳ $mes se actualiza automáticamente (computed)            │ │
│  │    ↳ Dispara loadEspecialistasAction() automáticamente       │ │
│  ├───────────────────────────────────────────────────────────────┤ │
│  │ Gestión de Especialistas:                                     │ │
│  │  • loadEspecialistasAction(page)                              │ │
│  │    ↳ Carga especialistas con paginación                      │ │
│  │    ↳ Actualiza $especialistas, $pagination                   │ │
│  │  • selectEspecialistaAction(especialista, hora)               │ │
│  │    ↳ Actualiza $bookingDraft.especialista y .hora            │ │
│  ├───────────────────────────────────────────────────────────────┤ │
│  │ Paginación:                                                   │ │
│  │  • changePageAction(page)                                     │ │
│  │    ↳ Llama a loadEspecialistasAction(page)                   │ │
│  ├───────────────────────────────────────────────────────────────┤ │
│  │ Navegación:                                                   │ │
│  │  • goToConfirmationAction()                                   │ │
│  │    ↳ Valida especialista y hora                              │ │
│  │    ↳ Navega a ConfirmationForm                               │ │
│  ├───────────────────────────────────────────────────────────────┤ │
│  │ Confirmación:                                                 │ │
│  │  • confirmReservaAction()                                     │ │
│  │    ↳ Valida datos requeridos                                 │ │
│  │    ↳ Verifica límite semanal (hasWeeklyBookingForService)    │ │
│  │    ↳ Llama a createReserva API                               │ │
│  │    ↳ Maneja $uiState.loading y .error                        │ │
│  │    ↳ Redirige tras éxito                                     │ │
│  │    ↳ Llama a resetBookingAction()                            │ │
│  ├───────────────────────────────────────────────────────────────┤ │
│  │ Reset:                                                        │ │
│  │  • resetBookingAction()                                       │ │
│  │    ↳ Limpia todos los maps y átomos                          │ │
│  │    ↳ Mantiene $services para evitar recargas                 │ │
│  └───────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              │ usa
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        BOOKINGS API (bookingsApi.js)                │
├─────────────────────────────────────────────────────────────────────┤
│  • getServices()                                                    │
│  • getEspecialistasDisponibles(serviceId, fecha, limit, offset)    │
│  • getUserBookings()                                                │
│  • createReserva(reservaData)                                       │
│  • getCurrentUser()                                                 │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              │ llama
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│                          BACKEND API                                │
│  /api/services                                                      │
│  /api/especialistas/disponibles                                     │
│  /api/reservas (GET, POST)                                          │
│  /api/me                                                            │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Flujo de Datos Detallado

### 1️⃣ Flujo: Selección de Servicio

```
┌──────────────┐
│   Usuario    │
│ hace clic en │
│   servicio   │
└──────┬───────┘
       │
       ▼
┌─────────────────────────────┐
│  Service Component          │
│  onSelect(service)          │
└──────┬──────────────────────┘
       │
       ▼
┌─────────────────────────────┐
│  selectServiceAction()      │
│  ┌─────────────────────────┐│
│  │ $bookingDraft.setKey(  ││
│  │   "service", service   ││
│  │ )                      ││
│  │ $estado.set(           ││
│  │   "DateForm"           ││
│  │ )                      ││
│  └─────────────────────────┘│
└──────┬──────────────────────┘
       │
       ▼
┌─────────────────────────────┐
│  UI se actualiza            │
│  automáticamente            │
│  (Preact reactivity)        │
└─────────────────────────────┘
```

### 2️⃣ Flujo: Selección de Día

```
┌──────────────┐
│   Usuario    │
│ selecciona   │
│     día      │
└──────┬───────┘
       │
       ▼
┌─────────────────────────────┐
│  Calendario Component       │
│  handleDiaChange(dia)       │
└──────┬──────────────────────┘
       │
       ▼
┌─────────────────────────────┐
│  setDiaAction(dia)          │
│  ┌─────────────────────────┐│
│  │ $bookingDraft.setKey(  ││
│  │   "dia", dia           ││
│  │ )                      ││
│  │                        ││
│  │ $pagination.setKey(    ││
│  │   "current", 1         ││
│  │ )                      ││
│  │                        ││
│  │ await                  ││
│  │ loadEspecialistasAction││
│  │   ()                   ││
│  └─────────────────────────┘│
└──────┬──────────────────────┘
       │
       ▼
┌─────────────────────────────┐
│  $mes se actualiza          │
│  automáticamente            │
│  (computed store)           │
└──────┬──────────────────────┘
       │
       ▼
┌─────────────────────────────┐
│  loadEspecialistasAction()  │
│  ┌─────────────────────────┐│
│  │ Validar servicio y día ││
│  │ Llamar API             ││
│  │ $especialistas.set()   ││
│  │ $pagination.setKey()   ││
│  └─────────────────────────┘│
└──────┬──────────────────────┘
       │
       ▼
┌─────────────────────────────┐
│  UI se actualiza            │
│  - Calendario muestra día   │
│  - Lista de especialistas   │
│  - Paginación               │
└─────────────────────────────┘
```

### 3️⃣ Flujo: Confirmación de Reserva

```
┌──────────────┐
│   Usuario    │
│ hace clic en │
│  "Confirmar" │
└──────┬───────┘
       │
       ▼
┌─────────────────────────────┐
│  ConfirmationActions        │
│  onConfirmar()              │
└──────┬──────────────────────┘
       │
       ▼
┌─────────────────────────────┐
│  confirmReservaAction()     │
│  ┌─────────────────────────┐│
│  │ 1. Validar datos       ││
│  │ 2. $uiState.setKey(    ││
│  │      "loading", true   ││
│  │    )                   ││
│  │ 3. getUserBookings()   ││
│  │ 4. Validar límite      ││
│  │    semanal             ││
│  │ 5. createReserva()     ││
│  │ 6. $uiState.setKey(    ││
│  │      "loading", false  ││
│  │    )                   ││
│  │ 7. resetBookingAction()││
│  │ 8. Redirigir           ││
│  └─────────────────────────┘│
└──────┬──────────────────────┘
       │
       ├─── Si error ───┐
       │                │
       ▼                ▼
┌─────────────┐  ┌─────────────┐
│   Éxito     │  │    Error    │
│             │  │             │
│ Redirige a  │  │ Muestra     │
│ /user/      │  │ mensaje en  │
│ reservas    │  │ StatusAlert │
└─────────────┘  └─────────────┘
```

---

## 🧩 Componentes Presentacionales

```
┌─────────────────────────────────────────────────────────────────────┐
│                         RUTAS (Presentacionales)                    │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ServiceForm                                                        │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │ Consume:                                                      │ │
│  │  • $services                                                  │ │
│  │  • $bookingDraft                                              │ │
│  │                                                               │ │
│  │ Renderiza:                                                    │ │
│  │  • Grid de componentes Service                               │ │
│  │                                                               │ │
│  │ Conecta:                                                      │ │
│  │  • Service.onSelect → selectServiceAction                    │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                                                     │
│  DateForm                                                           │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │ Consume:                                                      │ │
│  │  • $bookingDraft                                              │ │
│  │  • $especialistas                                             │ │
│  │  • $pagination                                                │ │
│  │  • $mes                                                       │ │
│  │                                                               │ │
│  │ Renderiza:                                                    │ │
│  │  • Calendario                                                 │ │
│  │  • EspecialistasList                                          │ │
│  │                                                               │ │
│  │ Conecta:                                                      │ │
│  │  • Calendario.handleMesChange → setMesAction                 │ │
│  │  • Calendario.handleDiaChange → setDiaAction                 │ │
│  │  • EspecialistasList.onSelectHora → selectEspecialistaAction │ │
│  │  • EspecialistasList.onPageChange → changePageAction         │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                                                     │
│  ConfirmationForm                                                   │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │ Consume:                                                      │ │
│  │  • $userName                                                  │ │
│  │  • useReservas() hook                                         │ │
│  │                                                               │ │
│  │ Renderiza:                                                    │ │
│  │  • ResumenCita                                                │ │
│  │  • StatusAlert                                                │ │
│  │  • ConfirmationActions                                        │ │
│  │                                                               │ │
│  │ Conecta:                                                      │ │
│  │  • ConfirmationActions.onConfirmar → confirmarReserva        │ │
│  │  • ConfirmationActions.onModificar → $estado.set("DateForm") │ │
│  └───────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🔌 Hook Simplificado

```
┌─────────────────────────────────────────────────────────────────────┐
│                    useReservas Hook                                 │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  Suscripciones:                                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │ const booking = useStore($bookingDraft)                       │ │
│  │ const uiState = useStore($uiState)                            │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                                                     │
│  Retorna:                                                           │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │ {                                                             │ │
│  │   selectedService: booking.service,                           │ │
│  │   dia: booking.dia,                                           │ │
│  │   selectedEspecialista: booking.especialista,                 │ │
│  │   selectedHora: booking.hora,                                 │ │
│  │   loading: uiState.loading,                                   │ │
│  │   error: uiState.error,                                       │ │
│  │   confirmarReserva: confirmReservaAction                      │ │
│  │ }                                                             │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                                                     │
│  ✅ Sin useState                                                    │
│  ✅ Sin useEffect                                                   │
│  ✅ Sin lógica de negocio                                           │
│  ✅ Solo puente a la store                                          │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 📊 Comparación Antes vs Después

### ANTES: Arquitectura Dispersa

```
Component
    │
    ├─ useState (loading, error)
    ├─ useEffect (cargar datos)
    ├─ Lógica de negocio
    ├─ Validaciones
    ├─ Llamadas a API
    └─ Renderizado
```

### DESPUÉS: Arquitectura Centralizada

```
Component (Presentacional)
    │
    └─ Renderizado

Hook (Puente)
    │
    └─ Suscripciones a stores

Store (Lógica)
    │
    ├─ Estado (maps, atoms, computed)
    ├─ Acciones (lógica de negocio)
    └─ Validaciones

API Layer
    │
    └─ Llamadas HTTP
```

---

## 🎯 Principios de Diseño Aplicados

1. **Single Responsibility Principle**

   - Componentes: Solo presentación
   - Hook: Solo puente
   - Store: Solo lógica de estado
   - API: Solo comunicación HTTP

2. **Don't Repeat Yourself (DRY)**

   - Lógica centralizada en acciones
   - Computed stores para valores derivados
   - Maps para agrupar datos relacionados

3. **Separation of Concerns**

   - UI separada de lógica
   - Estado separado de presentación
   - API separada de componentes

4. **Single Source of Truth**
   - Store es la única fuente de verdad
   - No duplicación de estado
   - Sincronización automática

---

**Arquitectura diseñada para ser escalable, mantenible y testeable** ✨
